Pour creer une nouvelle fonctionnalite, il faut :
- API (Raha mila API)
- Creation du page "src/features/customers/pages/"
- Routage 
- Creer un compononent (si on utilise formulaire,boutton ...)
- Service (Logique metier)

Jours 0 ==> 06/05/2026
- lister les tables a supprimer les donnees (via checbox)
    - appel API (OK)
    - liste des tables (donnees brutes) (OK)
    - selectionner les modules a supprimer (donnees) (OK)
    - get modulesSelectionner.nom et 
      http://localhost/prestashop/api/nom (appel de fonction de
    reinitialisation) (OK)
    - tester (OK)
  Done ==> 07/05/2026

- sidebar : (OK)
  Constante (sidebar) -> Page -> AppRouter (app) (OK)

- import csv (google sheet) (OK)
    - import csv des tables et modules essentiels (OK)

DEADLINE : 09/05/2025

- creation des donnees csv a importer
- completion des moyens d'import des fichiers 
  dans le fichiers "moduleRegistry.js", ajouter tout les modules
  a fin d'importer pleins de fichier.
- maitrise du code (Au Nom de Jesus)

Utilisateur clique "🗑️ Supprimer"
    ↓
handleDeleteProduct() s'exécute
    ↓
Confirmation : "Êtes-vous sûr ?"
    ↓ (Si OUI)
setIsDeleting(true) ← Désactiver UI
    ↓
await deleteModuleRecord("products", 123)
    ↓
API PrestaShop supprime le produit
    ↓
setProducts(filter) ← Enlever de la liste
    ↓
Message de succès en vert
    ↓
Après 3s → Message disparaît

DEADLINE : 10/05/2025

Jours 1 :
Backoffice :
    - login
        - creation de connection de donnee brute (OK)
        - localhost:5173 ==> FronOffice (OK)
        - localhost:5173/admin ==> Backoffice (OK)
        
    - reinitialisation (un seul boutton)
    - preparation import pour les 4 fichiers 
        - adapter l'import par rapport au fichier donner
    - liste commande (+ boutton modification d'etat)

FronOffice :
    - page d'accueil pour afficher les produits
        i- avec fiche produit
    - faire marcher le workflow d'achat
        i- panier
        ii- validation commande de livraison
    - etat des commandes